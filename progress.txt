VISUAL LEARNING - PROGRESS TRACKER
===================================
Updated: 2026-05-19

=== CURRENT STATE ===
15 stages live, 9 tracks, retro clean UI
Build: 458 modules, 604 KB / 172 KB gzip

=== COMPLETED PHASES ===

PHASE 0: SYSTEM DESIGN SIMULATOR (Stages 1-6)
✓ 6 stages with particle-based metrics visualization
✓ Fix-mode progressions (problem → fix 1 → fix 2 → optimized)
✓ Live metrics HUD (RPS, latency, CPU, pool, cache hit)
✓ Health state visualization (green/amber/red pulse rings)

PHASE 1: REDIS KV STORE (Stage 7)
✓ Hash table with 8 buckets, LRU eviction
✓ Three persistence modes: none, RDB, AOF
✓ Crash recovery with AOF replay
✓ Bug fixes: fixModeIndex sync, AOF log doubling, entry positioning

PHASE 2: CASSANDRA LSM TREE (Stage 8)
✓ WAL → Memtable → L0/L1 SSTables
✓ Bloom filters (8-bit, 2-hash), compaction
✓ Read path: Memtable → Bloom check → SSTable scan

PHASE 3: POSTGRESQL B+ TREE (Stage 9)
✓ Order-3 B+ tree with leaf/internal splits
✓ Two-pass layout algorithm (leaves by rightLink chain, internals by child midpoint)
✓ Range scan via leaf chain

PHASE 4: LLM TRANSFORMER (Stage 10)
✓ GPT-style decoder: tokenize → embed → 2 attention layers → FFN → softmax
✓ Real math: matVec, layerNorm, GELU, softmax, causal mask
✓ DIM=8, NUM_HEADS=2, NUM_LAYERS=2, FFN_DIM=32, VOCAB=20
✓ Deterministic weights (LCG seed=42)
✓ Fixed input "The cat sat on", animated forward pass (600ms/phase)
✓ 3 fix modes: Attention heatmap, FFN neurons/GELU, Prediction/temperature
✓ Sidebar toggle buttons (not sequential progression)

PHASE 5: UI REDESIGN — Retro Clean Palette
✓ Color scheme: dark CRT → sage/olive bg (#C5C6A8), cream surfaces (#F0F0E8), coral accent (#D4654A)
✓ Typography: added Space Mono for body, pixel font for labels
✓ Buttons: retro pill with gold double border (.retro-btn)
✓ Navigation: flat grid → collapsible folder tree
✓ Window chrome: retro blue title bars, colored controls
✓ ALL simulation views updated to retro palette
✓ Sidebar + InfoCard restyled

PHASE 6: LANDING PAGE CONTENT
✓ Value Proposition section (See It / Break It / Fix It)
✓ Who Is This For section (Interview Prep / CS Students / Engineers)
✓ FAQ section (7 SEO-optimized questions)
✓ Final CTA section + Footer
✓ Stats bar + SEO meta tags

PHASE 7: SCROLL ANIMATIONS
✓ Shared IntersectionObserver (fire-once, data-animate)
✓ CSS keyframes: section-fade-up, progress-load, monitor-flicker, scanline-pass
✓ Value Prop: progress bar + window cascade (150ms stagger)
✓ FAQ: monitor flicker, CTA: scanline sweep, Footer: fade-up
✓ prefers-reduced-motion support
✓ skipHero: all sections revealed immediately on ESC back

PHASE 8: TCP 3-WAY HANDSHAKE (Stage 11)
✓ Client/server state machine: CLOSED → SYN_SENT → ESTABLISHED → FIN_WAIT → TIME_WAIT
✓ SYN/SYN-ACK/ACK packet animation
✓ Fix modes: SYN timeout (exponential backoff), RST, connection close & TIME-WAIT

PHASE 9: APACHE KAFKA (Stage 12)
✓ Progressive reveal pattern — each fix mode adds one layer:
  - Base: simple append-only log (producer → log → consumer)
  - Fix 0: partitions with hash routing (key → partition)
  - Fix 1: broker replication (leader/follower, ISR, acks=1 vs acks=all)
  - Fix 2: broker failure & leader election (kill/revive, controller)
  - Fix 3: consumer group rebalancing (add/remove consumers)
✓ Controls scoped to current mode — no information overload

PHASE 10: RATE LIMITING (Stage 13)
✓ Progressive reveal — 5 algorithms across fix modes:
  - Base: no limiter, server crash demo
  - Fix 0: Token Bucket (capacity + refill rate, allows burst)
  - Fix 1: Sliding Window Log (precise but O(N) memory)
  - Fix 2: Fixed Window Counter (cheap but 2x burst at boundary — thundering herd demo)
  - Fix 3: Sliding Window Counter (weighted previous + current window)
  - Fix 4: Leaky Bucket (FIFO queue, constant drain rate, smoothing)
✓ Mode-specific control sliders (capacity, refill, drain, window, limit)
✓ Auto-send + manual burst trigger for fixed-window thundering herd
✓ Live counters: total/allowed/rejected/queued

PHASE 11: SHAZAM AUDIO FINGERPRINTING (Stage 14)
✓ Progressive reveal — full audio recognition pipeline:
  - Base: animated composite waveform (why raw matching fails)
  - Fix 0: Spectrogram (24×16 grid, energy color-coded, builds left-to-right)
  - Fix 1: Constellation Map (local maxima peak detection, threshold slider)
  - Fix 2: Fingerprint Hashing (anchor + target zone, (f₁,f₂,Δt) → 32-bit hash, formula display)
  - Fix 3: Database Lookup (5 fake songs, hash matching, per-song bars)
  - Fix 4: Time-Offset Histogram (correct song spikes, others scatter, MATCH FOUND)
✓ Deterministic data: 5 songs (Electronic Beat, Piano Melody, Rock Guitar, Jazz Trumpet, Pop Vocal)
✓ Query = noisy shifted subset of Rock Guitar (guarantees match demo)
✓ Pure math (no Web Audio API): gaussian energy spread, 3×3 local maxima, LCG random

PHASE 12: SHARDING vs PARTITIONING (Stage 15)
✓ Split-screen side-by-side: LEFT one-server PARTITIONING, RIGHT N-server SHARDING
✓ Same key set (12 keys), same hash(key) % 4 — different consequences per mode
✓ Progressive reveal — 6 fix modes target one engineering dimension each:
  - Base: topology reveal (1 process/disk/failure-domain vs N)
  - Fix 0: write path — hash & place (function call vs network RTT)
  - Fix 1: point read — single key (both prune, sharding's best case)
  - Fix 2: range / scatter — sequential scan vs N-shard fan-out
  - Fix 3: failure — kill server (total outage) vs kill shard (partial outage)
  - Fix 4: atomic transfer — single-node ACID vs 2PC (PREPARE→VOTE→COMMIT→ACK, scrolling log)
  - Fix 5: scale wall — load slider 0-200K rps; LEFT 1-server hockey-stick latency + DISK FULL vs RIGHT 4-shard flat (why sharding wins on capacity)
✓ Per-side metrics: writes/reads, OK/fail, network RTTs, bytes-over-network, last latency
✓ Particle interpolation with kind-coded colors; failed particles stall at 40% with X
✓ Mode-gated controls: insert/auto-insert, point/range inputs, kill/revive nodes, txn trigger, load slider + START RAMP toggle

=== BUG FIXES ===

Redis: fixModeIndex sync, AOF doubling, eviction scope, RDB timing
B+ Tree: layout x=0 bug, edge routing, leaf rightLink arrows
Transformer: unused imports/variables
Kafka: TS6133 unused parts/slot
Rate Limiter: missing useState import
Shazam: unused rng const in defineSongs
UI: retro-window opacity blocking in revealed scroll sections

=== PLANNED (Stages 16+) ===

Stage 16: Virtual Memory & Page Faults — TLB, page table walk, page replacement
Stage 17: Raft Consensus — leader election, log replication, split brain
Stage 18: Event Loop & Task Queues — call stack, microtask/macrotask, Promise vs setTimeout
Stage 19: Consistent Hashing — ring, virtual nodes, rebalancing
Stage 20: CRDT / Vector Clocks — conflict-free replication, causality

=== FILES ===

Modules: 27 files (9 modules × 3 files)
  src/modules/{redis,lsm,btree,transformer,tcp,kafka,rate-limiter,shazam,sharding}/ — types, engine, simulation each
